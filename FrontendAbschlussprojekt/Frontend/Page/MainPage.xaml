<Page
    x:Class="Frontend.Page.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Frontend.Page"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainPage"
    d:DesignHeight="450"
    d:DesignWidth="800"
    Background="#FFFFFF"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30px" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100px" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="100px" />
            </Grid.ColumnDefinitions>

            <TextBlock
                x:Name="TxtSearch"
                Grid.Column="1"
                Background="LightGray"
                Text="" />
        </Grid>

        <DataGrid Grid.Row="1">
            <DataGrid.Columns>
                <DataGridTextColumn x:Name="DGTxtName" />
                <DataGridCheckBoxColumn x:Name="DGChkBox" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
